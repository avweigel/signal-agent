# TODO

- Migrate from system Python 3.9 to Homebrew Python 3.12. System Python 3.9 is
  EOL and google-auth has warned. Replace `/usr/bin/python3` references in
  launchd plists. Re-grant FDA to the new binary path.
